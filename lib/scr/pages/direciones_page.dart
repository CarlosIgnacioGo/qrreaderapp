import 'package:flutter/material.dart';

class DirrecionesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dirreciones'),
    );
  }
}